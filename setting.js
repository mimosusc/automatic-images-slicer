const CONFIG = {
  CANVAS_PADDING: {
    VERTICAL: 30, // 任意の数値 Number
    HORIZONTAL: 60, // 任意の数値 Number
    COLOR: 'rgb(0, 0, 255)', // 任意のHEXカラー String
  },
  COLOR_TOLERANCE: 12, // 色差異の許容値。劣化で発生する軽微な色の差異を無視する
  PREFIX_CONNECTION: '--', // プレフィックスとネームのコネクション
}

export default CONFIG
